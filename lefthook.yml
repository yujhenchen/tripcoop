pre-commit:
    parallel: true
    commands:
        format:
            root: "web"
            run: bun run format
        lint:
            root: "web"
            run: bun run lint

pre-push:
    commands:
        check:
            root: "web"
            run: bun run check
